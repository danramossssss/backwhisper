{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python whisper_api_fastapi.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}